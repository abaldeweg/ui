import { describe, it, expect } from 'vitest'

import BContainer from '../BContainer/BContainer.vue'

describe('BContainer', () => {
  it('shows BContainer', () => {
    expect(BContainer).toBeTruthy()
  })
})
