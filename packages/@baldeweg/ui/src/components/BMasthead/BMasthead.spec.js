import { describe, it, expect } from 'vitest'

import BMasthead from '../BMasthead/BMasthead.vue'

describe('BMasthead', () => {
  it('shows BMasthead', () => {
    expect(BMasthead).toBeTruthy()
  })
})
