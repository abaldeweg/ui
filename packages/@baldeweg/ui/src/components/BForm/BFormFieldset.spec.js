import { describe, it, expect } from 'vitest'

import BFormFieldset from '../BForm/BFormFieldset.vue'

describe('BFormFieldset', () => {
  it('shows BFormFieldset', () => {
    expect(BFormFieldset).toBeTruthy()
  })
})
