import { describe, it, expect } from 'vitest'

import BFormItem from '../BForm/BFormItem.vue'

describe('BFormItem', () => {
  it('shows BFormItem', () => {
    expect(BFormItem).toBeTruthy()
  })
})
