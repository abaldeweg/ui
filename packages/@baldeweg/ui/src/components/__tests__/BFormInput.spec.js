import { describe, it, expect } from 'vitest'

import BFormInput from '../BForm/BFormInput.vue'

describe('BFormInput', () => {
  it('shows BFormInput', () => {
    expect(BFormInput).toBeTruthy()
  })
})
