import { describe, it, expect } from 'vitest'

import BTabsLink from '../BTabs/BTabsLink.vue'

describe('BTabsLink', () => {
  it('shows BTabsLink', () => {
    expect(BTabsLink).toBeTruthy()
  })
})
