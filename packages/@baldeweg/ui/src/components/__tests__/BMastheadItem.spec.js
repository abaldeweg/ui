import { describe, it, expect } from 'vitest'

import BMastheadItem from '../BMasthead/BMastheadItem.vue'

describe('BMastheadItem', () => {
  it('shows BMastheadItem', () => {
    expect(BMastheadItem).toBeTruthy()
  })
})
