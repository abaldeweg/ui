import { describe, it, expect } from 'vitest'

import BFormSelect from '../BForm/BFormSelect.vue'

describe('BFormSelect', () => {
  it('shows BFormSelect', () => {
    expect(BFormSelect).toBeTruthy()
  })
})
