import { describe, it, expect } from 'vitest'

import BSubheader from '../BSubheader/BSubheader.vue'

describe('BSubheader', () => {
  it('shows BSubheader', () => {
    expect(BSubheader).toBeTruthy()
  })
})
