{
  "$schema": "./../component.schema.json",
  "name": "BNav",
  "props": [
    {
      "name": "nav",
      "description": "The array expects objects with properties route (String or RouterLink), title (String), badge (String), icon (object containing property type ('icon' or 'dot')) and value (String).",
      "type": [
        "Array"
      ]
    }
  ]
}
