import { describe, it, expect } from 'vitest'

import BBottomNavItem from '../BBottomNav/BBottomNavItem.vue'

describe('BBottomNavItem', () => {
  it('shows BBottomNavItem', () => {
    expect(BBottomNavItem).toBeTruthy()
  })
})
