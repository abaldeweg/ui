# Components
<%_ components.forEach((el)=>{ _%>

## <%= el.name %>

<%_ if (el.props) { _%>
### Props

|Name|Type|Default|Allowed Values|Example|Description
|----|----|-------|--------------|----------|-----------
<%_ el.props.forEach((prop)=>{ _%>
|<%= prop.name %> |<% if (prop.type) { %><%= prop.type.join(', ') %><% } %>|<%= prop.default %>|<% if (prop.allowedValues) { %><%= prop.allowedValues.join(', ') %><% } %>|<%= prop.description %>
<%_ }); _%>
<%_ }; _%>
<%_ if (el.slots) { _%>

### Slots

|Name|Description
|----|-----------
<%_ el.slots.forEach((slot)=>{ _%>
|<%= slot.name %>|<%= slot.description %>
<%_ }); _%>
<%_ }; _%>
<%_ if (el.events) { _%>

### Events

|Name|Description
|----|-----------
<%_ el.events.forEach((event)=>{ _%>
|<%= event.name %>|<%= event.description %>
<%_ }); _%>
<%_ }; _%>
<%_ }); _%>
