# Components
<%_ components.forEach((el)=>{ _%>

## <%= el.name %>

<%_ if (el.props && el.props.length > 0) { _%>
### Props

|Name      |Type      |Default   |Values    |Required  |
|----------|----------|----------|----------|----------|
<%_ el.props.forEach((prop)=>{ _%>
  |<%= prop.name _%>
  |<% if (prop.type) { _%>
    <%= prop.type.name.replace("|", ", ") _%>
  <% } _%>
  |<% if (prop.defaultValue) { _%>
    <% if (prop.defaultValue.func) { _%>func: <% } _%>
    <%= prop.defaultValue.value  _%>
  <% } _%>
  |<% if (prop.values) { %><%= prop.values.join(', ') %><% } _%>
  |<% if (prop.required) { %>  Yes <% } else { %>  No <% } %>|
<%_ }); _%>
<%_ }; _%>
<%_ if (el.slots && el.slots.length > 0) { _%>

### Slots

|Name|
|----|
<%_ el.slots.forEach((slot)=>{ _%>
|<%= slot.name %>|
<%_ }); _%>
<%_ }; _%>
<%_ if (el.events && el.events.length > 0) { _%>

### Events

|Name|
|----|
<%_ el.events.forEach((event)=>{ _%>
|<%= event.name %>|
<%_ }); _%>
<%_ }; _%>
<%_ }); _%>
